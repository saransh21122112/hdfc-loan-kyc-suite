# Web framework
fastapi==0.111.0
openai>=1.30.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
aiofiles==23.2.1

# Settings & validation
pydantic==2.7.1
pydantic-settings==2.2.1

# Database
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1

# Auth & security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==42.0.7

# OCR — primary backend
pytesseract==0.3.10
Pillow==10.3.0
pdf2image==1.17.0
opencv-python-headless==4.9.0.80
numpy<2  # cv2 4.9.x compiled against NumPy 1.x; NumPy 2.x breaks it

# OCR — upgrade path (comment out if not using GPU)
# paddlepaddle==2.6.1
# paddleocr==2.7.3

# ML / NLP
# spacy==3.7.4             # pip install then: python -m spacy download en_core_web_sm
rapidfuzz==3.9.0           # fuzzy name matching for cross-document validation

# Object storage (MinIO / S3-compatible)
boto3==1.34.100

# Async tasks
celery==5.4.0
redis==5.0.4

# Observability (audit trail — OpenTelemetry)
opentelemetry-sdk==1.24.0
opentelemetry-exporter-otlp==1.24.0
opentelemetry-instrumentation-fastapi==0.45b0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0
pytest-cov==5.0.0
